Mercury Windows SFX overlays
============================

This directory contains the three binary inputs that cannot be represented as
ordinary text patches:

- 7zSD.Win32.sfx: outer self-extractor used by x86 and x86-64 installers.
- 7zSD.ARM64.sfx: outer self-extractor used by ARM64 installers.
- setup.ico: Mercury icon embedded when the two executables are rebuilt.

Firefox deliberately checks prebuilt SFX executables into its source tree so
that packaging does not require Visual Studio. Mercury follows that model but
keeps all editable resource changes in
patches/firefox-153/170-mercury-windows-sfx-branding.patch. setup.sh copies
only these three files over the pinned Firefox tree; it does not replace the
7-Zip/LZMA SDK source or build projects.

Firefox stores resource.rc with CRLF line endings, so Git encodes patch 170 as
an exact binary delta. The decoded changes set CompanyName and LegalCopyright
to Alex313031, FileDescription to Mercury Browser Installer, ProductName to
Mercury, and the minimum-system message to Microsoft Windows 10 or newer.

<!-- BEGIN GENERATED SFX BUILD INFO -->
The current executables identify themselves as 7-Zip Self-extracting Archive
18.05 and were rebuilt from the patched Firefox 153 source tree on 2026-08-09.
The build used Firefox commit
`0c39e9282688363f5028d0541c17784f7fa5117c`, Mercury commit
`b80dd4d4b976cfb4787cc722352dfe00fb1add79`, MSBuild 17.14.51.32402, and
Windows SDK 10.0.22621.0. The corresponding GitHub Actions run is
https://github.com/gz83/Mercury/actions/runs/31299823531.

Current checked-in executable hashes:

    76050b1dc74e7a8daae13fdac18c0c3280547136e29a4c2a45790870c89ac247  7zSD.Win32.sfx
    a3d648e42be18d709256bc3feadb1de1785c02337d01dc1a9b0d9053ac0c3c4e  7zSD.ARM64.sfx
<!-- END GENERATED SFX BUILD INFO -->

Rebuild procedure
-----------------

The preferred rebuild path is the manually dispatched
`.github/workflows/rebuild-windows-sfx.yml` workflow. It verifies both outputs,
and uploads them with SHA256SUMS.txt, BUILDINFO.json, and build provenance. The
workflow never modifies the repository. Download its artifact, inspect the
metadata, and manually replace both checked-in SFX files. Preserve BUILDINFO.json
with the release records and update the generated hash block above. The
workflow pins Windows SDK 10.0.22621.0 because the project's v142 ARM64
compiler is incompatible with the newer 10.0.26100.0 headers selected by an
unqualified `WindowsTargetPlatformVersion=10.0`. For a local rebuild:

1. Run setup.sh against FIREFOX_153_0_3_RELEASE so patch 170 and setup.ico are
   present in the Firefox checkout.
2. Follow other-licenses/7zstub/README.mozilla in that checkout.
3. Open other-licenses/7zstub/firefox/SFXSetup.sln with the Visual Studio 2022
   C++ x86 and ARM64 toolchains.
4. Build the ReleaseD configuration for x86 and ARM64.
5. Replace 7zSD.Win32.sfx and 7zSD.ARM64.sfx in this directory.
6. Verify both files' version resources, icon, architecture, minimum-Windows
   message, and SHA-256 hashes. Then build and launch each Windows installer.

Regenerate both executables whenever Firefox changes any file under
other-licenses/7zstub, even if Firefox does not update its checked-in binaries
in the same commit.
